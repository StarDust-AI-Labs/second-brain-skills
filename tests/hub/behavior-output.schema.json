{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": [
    "intent", "contract_id", "required_chain", "executed_conditional_steps",
    "skipped_conditional_steps", "planned_tool_calls", "evidence",
    "required_outputs", "final_action", "side_effects_attempted", "blocked_reason", "progress_events"
  ],
  "properties": {
    "intent": {"type": "string"},
    "contract_id": {"type": ["string", "null"]},
    "required_chain": {"type": "array", "items": {"type": "string"}},
    "executed_conditional_steps": {"type": "array", "items": {"type": "string"}},
    "skipped_conditional_steps": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["id", "reason"],
        "properties": {"id": {"type": "string"}, "reason": {"type": "string", "minLength": 1}},
        "additionalProperties": false
      }
    },
    "planned_tool_calls": {"type": "array", "items": {"type": "string"}},
    "evidence": {"type": "array", "items": {"type": "string"}},
    "required_outputs": {"type": "array", "items": {"type": "string"}},
    "final_action": {"enum": ["create", "edit", "move-or-delete", "read", "advisory", "clarify", "none"]},
    "side_effects_attempted": {"type": "array", "items": {"type": "string"}, "maxItems": 0},
    "blocked_reason": {"type": ["string", "null"]},
    "progress_events": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["sequence", "display_id", "label", "state", "trace", "reason", "fallback"],
        "properties": {
          "sequence": {"type": "integer", "minimum": 1},
          "display_id": {"type": "string", "minLength": 1},
          "label": {"type": "string", "minLength": 1},
          "state": {"enum": ["completed", "skipped", "blocked"]},
          "trace": {"type": "string"},
          "reason": {"type": ["string", "null"]},
          "fallback": {"type": ["string", "null"]}
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
